# Production API when the UI is on Vercel and FastAPI is elsewhere (e.g. Render).
# No trailing slash. Set the same value in Vercel → Project → Settings → Environment Variables.
# VITE_BACKEND_ORIGIN=https://your-api.onrender.com
